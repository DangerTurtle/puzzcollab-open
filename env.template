TAP_URL=http://localhost:2480
APP_URL=http://127.0.0.1:3000
# Symmetric secret used to encrypt/decrypt puzzle answer keys (answerBlock).
# Any string works -- it's hashed into an AES-256 key. Required for puzzle
# authoring; never share it or commit a real value.
ANSWER_KEY_SECRET=
