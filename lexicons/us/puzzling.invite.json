{
  "lexicon": 1,
  "id": "us.puzzling.invite",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "description": "A shareable link, minted by a team member, for joining a team.",
      "record": {
        "type": "object",
        "required": ["team", "joinPolicy", "creatorDid", "createdAt"],
        "properties": {
          "team": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef"
          },
          "joinPolicy": {
            "type": "string",
            "knownValues": ["open", "membersOnly"],
            "description": "'open': redeeming this invite grants team membership. 'membersOnly': redemption is restricted somehow to existing members -- exact behavior undecided, not yet enforced anywhere; the value is indexed as-is pending that decision."
          },
          "creatorDid": {
            "type": "string",
            "format": "did",
            "description": "The member who minted this invite."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
