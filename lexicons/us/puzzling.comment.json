{
  "lexicon": 1,
  "id": "us.puzzling.comment",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "description": "A chat message about a puzzle, optionally attached to a specific clue. Scoped to the author and the puzzle; team/roster membership is a client-side filtering concern, not a protocol-level access grant over comments.",
      "record": {
        "type": "object",
        "required": ["puzzle", "text", "createdAt"],
        "properties": {
          "puzzle": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef"
          },
          "clueId": {
            "type": "string",
            "maxLength": 200,
            "description": "The opaque id of the clue this comment is attached to, within the puzzle. Absent for general chat about the puzzle."
          },
          "text": {
            "type": "string",
            "maxGraphemes": 1000,
            "maxLength": 10000
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
